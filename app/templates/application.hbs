{{#if this.session.currentUser}}
  <span>
    Logged in as {{this.session.currentUser.name}}
  </span>
{{/if}}

<ul>
  <li>
    <LinkTo @route="login">Login</LinkTo>
  </li>
  <li>
    <LinkTo @route="playlists">Playlists</LinkTo>
  </li>
</ul>

{{outlet}}